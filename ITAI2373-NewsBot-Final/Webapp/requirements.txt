# Core -- required to run the app with lightweight fallbacks for every module
flask>=3.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
gunicorn>=21.0

# Optional upgrades -- install any/all of these for full-model results
# instead of the lightweight fallbacks. Uncomment the ones you want.

# spacy>=3.7                 # real NER (Module A)
#   then run: python -m spacy download en_core_web_sm

# transformers>=4.35         # transformer sentiment + BART summarization (Modules A & B)
# torch>=2.1                 # required by transformers

# sentence-transformers>=2.2 # embedding-based semantic search (Module B)

# langdetect>=1.0.9          # statistical language detection (Module C)
